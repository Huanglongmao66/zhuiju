# 追剧APP【首页/发现/排行榜/找片/我的】五页面标准布局方案与开发注意事项

## 一、全局公共架构（五页面统一）

### 1\.1 整体结构

全局采用 **MainActivity \+ ViewPager2 \+ 5大Fragment \+ 底部导航栏** 架构

根布局固定结构：

- 沉浸式状态栏占位层

- ViewPager2（主体内容，承载5个主页Fragment，懒加载）

- 底部常驻导航栏（首页/发现/排行榜/找片/我的）

### 1\.2 全局强制规范（所有页面通用）

- 非播放页根布局统一使用 **ConstraintLayout**，层级扁平、测量高效

- 视频播放页Item统一**FrameLayout**，极致防卡顿

- 所有页面适配**刘海屏、挖孔屏、折叠屏、沉浸式状态栏**

- 所有图片封面固定比例：横屏16:9、竖屏9:16，禁止拉伸变形

- 所有列表开启**下拉刷新、上拉加载**统一封装

- Fragment全部开启懒加载，不可见时不请求网络、不渲染数据

- 全局禁止多层嵌套、禁止ScrollView嵌套RecyclerView（严重卡顿）

---

## 二、首页（短视频Feed流）布局方案【核心页】

### 2\.1 页面定位

抖音式上下滑动全屏沉浸式短视频播放，APP核心性能页面，优先级最高。

### 2\.2 布局选型

**外层：ViewPager2（竖直方向） \+ 内层Item：FrameLayout极简布局**

### 2\.3 标准层级结构（从下至上）

```Plain Text
ViewPager2(vertical)
└─ Item Fragment(FrameLayout)
   ├─ TextureView（视频全屏画布 9:16）
   ├─ DanmakuView（弹幕层）
   ├─ 点击播放/暂停遮罩
   ├─ 右侧悬浮功能区（头像、关注、点赞、评论、收藏、分享）
   └─ 底部文案区（标题、话题、背景音乐、作者信息）
```

### 2\.4 开发注意事项

- ViewPager2必须设置 **offscreenPageLimit = 1**，配合播放器池预加载

- 页面强制锁定**竖屏**，禁止横竖屏切换

- 全屏沉浸式，隐藏状态栏、导航栏，无黑边、无留白

- Item布局层级严格控制在 **3层以内**，保证60帧滑动

- 禁止任何ScrollView、嵌套RecyclerView，杜绝手势冲突

- UI控件全部悬浮，不挤压视频画布尺寸

- 单击视频暂停/播放、双击点赞手势精准识别，防误触

---

## 三、发现页（热门推荐/合集）布局方案

### 3\.1 页面定位

热门视频、推荐合集、活动Banner、种草内容，双列卡片信息流。

### 3\.2 布局选型

**ConstraintLayout \+ 顶部轮播 \+ 分类Tab \+ 双列Grid RecyclerView**

### 3\.3 标准层级结构

```Plain Text
ConstraintLayout
├─ 顶部Banner轮播（ViewPager）
├─ 横向分类Tab栏（可滚动）
├─ 分割线/标题
└─ RecyclerView（Grid 2列）
   └─ Item：视频卡片（封面+播放图标+标题+热度+作者）
```

### 3\.4 开发注意事项

- 双列卡片**统一固定比例9:16**，界面整齐统一

- 卡片添加圆角、阴影、按压效果，提升质感

- Banner、Tab、列表分层隔离，互不冲突

- 图片预加载、占位图、错误图兜底，避免空白闪烁

- 列表滑动流畅优先级高于复杂动画，禁止卡片过多动效

- Item点击统一跳转短视频播放页，携带视频ID索引

---

## 四、排行榜页（热度榜单）布局方案

### 4\.1 页面定位

日榜/周榜/月榜视频热度排名，有序榜单展示，结构规整、信息层级清晰。

### 4\.2 布局选型

**ConstraintLayout \+ 顶部TabLayout \+ 单列RecyclerView**

### 4\.3 标准层级结构

```Plain Text
ConstraintLayout
├─ 顶部Tab（日榜/周榜/月榜）
├─ 榜单标题、更新时间
└─ 单列RecyclerView
   └─ Item：横向布局（排名+头像+昵称+封面+热度+关注按钮）
```

### 4\.4 开发注意事项

- 前三名特殊样式：金牌/银牌/铜牌图标替代数字排名

- 列表Item高度统一，避免参差不齐

- Tab切换快速刷新数据源，禁止重复请求、禁止页面重建

- 榜单数据缓存本地，二次进入快速展示、再刷新

- 排名数值、热度文案醒目突出，信息层级清晰

- 支持下拉刷新榜单、上拉加载更多尾部数据

---

## 五、找片页（搜索\+分类）布局方案

### 5\.1 页面定位

全站搜索、热门搜索、全品类分类、分区视频推荐，内容最多、模块最多。

### 5\.2 布局选型

**ConstraintLayout \+ 竖向ScrollView \+ 多组横向RecyclerView**

### 5\.3 标准层级结构

```Plain Text
ConstraintLayout
├─ 顶部搜索框（搜索、清空、历史搜索）
├─ 热门搜索标签流式布局
├─ 全部分类横向列表（游戏/生活/美食/影视等）
├─ 分区1：标题 + 横向视频卡片列表
├─ 分区2：标题 + 横向视频卡片列表
└─ 分区3：标题 + 横向视频卡片列表
```

### 5\.4 开发注意事项

- **严禁竖向RecyclerView嵌套横向RecyclerView**，采用ScrollView承载所有分区

- 所有横向列表开启复用、预加载，流畅滑动

- 搜索状态与浏览状态UI自动切换：搜索后隐藏所有分类，只展示结果列表

- 热门搜索、历史搜索独立缓存，本地持久化

- 分类选中状态高亮、主色标记，视觉反馈明确

- 分区模块懒加载，滑动到可视区域再加载数据，减少启动卡顿

---

## 六、我的页面（个人中心）布局方案

### 6\.1 页面定位

用户资料、粉丝数据、作品/收藏/喜欢/私密作品列表，个人中心核心页。

### 6\.2 布局选型

**CoordinatorLayout \+ CollapsingToolbar折叠头部 \+ TabLayout \+ ViewPager2 \+ 双列Grid列表**

### 6\.3 标准层级结构

```Plain Text
CoordinatorLayout
├─ 折叠头部（背景大图+头像+昵称+简介+数据统计）
├─ Tab栏（作品/私密/收藏/喜欢）
└─ ViewPager2
   └─ 子Fragment：双列Grid RecyclerView（视频作品卡片）
```

### 6\.4 开发注意事项

- 折叠头部滑动联动：上滑收缩、下拉展开，动效流畅不卡顿

- 用户数据（获赞、关注、粉丝）UI排版整齐，数值醒目

- Tab与ViewPager2双向联动，滑动、点击同步

- 作品列表统一9:16双列卡片，和发现页视觉统一

- 无作品、无收藏、无数据时展示空状态页面

- 头像、背景图支持圆角、模糊、适配沉浸式

- 设置、夜间模式、帮助中心等功能入口统一排布底部

---

## 七、五页面布局选型终极对照表

|页面|核心布局组件|布局特点|核心禁忌|
|---|---|---|---|
|首页|竖直ViewPager2 \+ FrameLayout|极简层级、全屏沉浸式、60帧流畅|禁止嵌套、禁止复杂测量|
|发现页|ConstraintLayout \+ 双列Grid|卡片信息流、视觉饱满|禁止卡片尺寸混乱、比例错乱|
|排行榜|ConstraintLayout \+ 单列List|结构规整、排名清晰|禁止Item高度混乱、排版杂乱|
|找片|ScrollView \+ 多横向List|多模块聚合、搜索分类一体|禁止列表嵌套列表|
|我的|Coordinator折叠头部 \+ Grid|动态折叠、多Tab内容切换|禁止头部卡顿、Tab联动失效|

## 八、全局UI统一硬性规范

- 所有页面圆角统一：12dp（卡片）、8dp（按钮）、4dp（标签）

- 所有页面间距统一：10dp、15dp、20dp三档标准间距

- 加载、空数据、错误、重试状态全局统一UI

- 动画统一200ms/250ms/300ms三档，无超长动画

- 主题色、文字色、透明度全局复用color资源，无硬编码色值

> （注：部分内容可能由 AI 生成）
