# Kotlin\+Jetpack\+ExoPlayer\+FFmpeg\+DanmakuFlameMaster\+OkHttp\+AES视频APP开发文档（注意事项\+开发任务\+分阶段开发）

## 一、技术栈整体定位

本方案为**Android原生商用级视频播放APP专属技术栈**，适配长视频、短视频、本地视频播放，支持弹幕、网络点播、离线缓存、视频格式兼容、传输与资源加密，兼顾性能、兼容性、安全性与可维护性。

核心组件分工：

- **Kotlin\+Jetpack**：项目基础架构、页面生命周期管理、状态管理、组件化适配

- **ExoPlayer**：核心播放器内核，负责流媒体解码、播放控制、自适应码率、离线缓存

- **FFmpeg**：辅助音视频处理，格式兼容、视频截取、转码、帧提取、异常视频修复

- **DanmakuFlameMaster**：B站开源弹幕渲染框架，实现弹幕加载、滚动、屏蔽、层级适配

- **OkHttp**：网络请求、视频分片下载、断点续传、请求拦截

- **AES加密**：视频资源分片加密、本地缓存文件加密、接口数据加密，防止资源盗用

## 二、全局统一开发注意事项（避坑核心）

### 2\.1 工程与Jetpack架构注意事项

- 全程采用**Kotlin协程\+Flow**处理异步逻辑，禁止使用原生Thread、Handler，避免播放状态错乱、内存泄漏。

- 严格遵循**MVVM架构**，View层只负责UI展示，播放逻辑、网络逻辑、缓存逻辑全部放在ViewModel/Repository层，杜绝页面臃肿。

- 利用Jetpack Lifecycle绑定播放器生命周期，页面销毁、后台切前台、横竖屏切换时自动暂停/释放播放器，杜绝**后台音频残留、内存泄漏**。

- 使用DataBinding/ViewBinding绑定布局，禁止findViewById，减少空指针异常。

- Jetpack Compose与原生XML布局混用时，播放器SurfaceView/TextureView需单独做层级适配，避免黑屏、画面闪烁。

- 全局统一状态管理，播放状态（播放、暂停、缓冲、出错）、网络状态、缓存状态统一通过StateFlow分发，避免多页面状态不同步。

### 2\.2 ExoPlayer播放器核心注意事项

- **禁止频繁新建ExoPlayer实例**：全局单例播放器，页面复用，切换视频只切换MediaItem，大幅减少卡顿与初始化耗时。

- 区分**硬解码/软解码**：默认开启硬件解码，针对老旧机型、特殊编码视频自动降级FFmpeg软解码，避免播放失败、花屏。

- 支持流媒体协议兼容：必须适配HLS、DASH、MP4、FLV主流格式，在线视频优先HLS自适应码率。

- 缓存策略优化：使用ExoPlayer DownloadManager实现分片缓存，禁止整文件下载，支持断点续传，缓存目录独立隔离，避免被系统清理。

- 播放控制完整性：必须实现倍速播放（0\.5x\-2x）、进度拖拽、静音、循环播放、记忆播放位置、手势调节亮度/音量。

- 异常重试机制：网络波动、视频加载失败、解码异常时，自动重试3次，失败后展示友好错误弹窗，不崩溃。

- 横竖屏适配：播放器全屏切换时锁定屏幕方向，重新适配Surface画布，避免画面拉伸、黑屏、进度条重置。

### 2\.3 FFmpeg集成注意事项

- 不替换ExoPlayer内核，仅做**辅助能力补充**：ExoPlayer负责常规播放，FFmpeg负责奇葩编码兼容、视频截取、帧图片提取、短视频裁剪、视频修复。

- ABI适配：必须适配armeabi\-v7a、arm64\-v8a主流架构，放弃废弃x86架构，减少包体积。

- 子线程执行FFmpeg命令：所有转码、解析操作放在独立协程子线程，严禁主线程执行，避免ANR。

- 资源及时释放：FFmpeg执行完毕后立即销毁会话、释放native内存，长期运行避免内存堆积OOM。

### 2\.4 DanmakuFlameMaster弹幕注意事项

- 弹幕View层级低于播放器Surface，避免遮挡视频画面，同时高于底部控制栏，保证视觉层级合理。

- 弹幕与播放进度**精准同步**：视频拖拽、倍速播放、暂停时，弹幕时间轴同步刷新，杜绝弹幕错位、堆积、超前播放。

- 性能优化：限制单屏最大弹幕数量，高频弹幕自动节流，低端机型降低弹幕渲染帧率，避免画面卡顿。

- 支持基础交互：弹幕屏蔽、颜色切换、透明度调节、弹幕开关、历史弹幕预加载。

- 页面销毁时清空弹幕队列、停止渲染线程，防止内存泄漏。

### 2\.5 OkHttp网络层注意事项

- 统一封装OkHttp拦截器：请求头统一配置、Token携带、超时统一处理、日志打印、异常拦截。

- 视频分片下载单独配置超时：流媒体下载超时大于普通接口，避免大视频加载超时失败。

- 开启OkHttp连接池复用，减少频繁创建连接带来的延迟。

- 网络状态监听：结合Jetpack ConnectivityManager，无网络、弱网、WiFi/流量切换时实时提示用户，自动暂停播放。

- 禁止UI线程调用网络请求，所有网络逻辑通过Repository层协程调度。

### 2\.6 AES加密安全注意事项（核心安全）

- 采用**AES\-128\-CBC**商用加密模式，禁止ECB不安全模式，配置固定IV偏移量，密钥私有化部署，禁止硬编码明文密钥。

- 双重加密：**传输加密\+本地缓存文件加密**，网络传输视频分片加密，本地缓存视频文件二次加密存储，防止本地扒资源。

- 密钥动态下发：APP启动后从服务端拉取临时密钥，密钥定时过期更新，杜绝固定密钥被破解。

- 加密分片处理：视频不整体加密，采用分片加密、边解密边播放，无需完整解密文件，提升播放速度与安全性。

- 禁止解密后明文落地：内存解密播放，不保存明文视频文件，杜绝资源泄露。

### 2\.7 性能与兼容通用注意事项

- 适配Android 10\+分区存储机制，视频缓存、本地视频读取严格遵循分区存储规范，避免权限报错。

- 后台播放适配：开启前台Service，适配系统后台权限限制，避免后台播放被系统杀死。

- 功耗优化：播放时自动适配屏幕常亮，暂停后关闭常亮；闲置状态释放播放器资源。

- 兼容高低端机型：高端机开启4K、高帧率播放，低端机自动降分辨率、关闭高耗能滤镜与动画。

## 三、全量核心开发任务清单

### 3\.1 项目架构与基础搭建任务

- 新建Kotlin Android项目，配置Gradle依赖，引入Jetpack全系组件（Lifecycle、ViewModel、Flow、DataBinding、Navigation）

- 集成OkHttp、统一网络封装、异常处理、日志拦截、网络状态监听

- 封装全局AES加密工具类、密钥管理工具、分片加解密工具

- 统一项目工具类、权限管理、日志工具、吐司工具、异常捕获工具

- 搭建MVVM分层架构：BaseActivity/BaseFragment、BaseViewModel、Repository通用模板

### 3\.2 ExoPlayer播放器核心开发任务

- 全局单例ExoPlayer封装，统一播放器管理类，统一初始化、释放、重置逻辑

- 实现基础播放功能：播放、暂停、停止、进度拖拽、上一集/下一集切换

- 高级播放功能：倍速播放、静音、循环播放、记忆播放位置

- 流媒体适配：HLS/DASH/本地视频多格式兼容

- 离线缓存功能：分片下载、断点续传、缓存进度展示、缓存管理、缓存清理

- 播放异常处理：网络失败、解码失败、资源失效重试机制

- 横竖屏全屏切换、手势控音量/亮度/进度功能开发

### 3\.3 FFmpeg辅助功能开发任务

- FFmpeg so库集成、架构适配、环境初始化

- 异常视频格式兼容播放适配

- 视频帧截图、封面提取功能开发

- 短视频裁剪、片段截取、格式转换辅助功能

### 3\.4 弹幕功能开发任务

- DanmakuFlameMaster框架集成、弹幕View布局适配

- 弹幕数据解析、时间轴匹配、实时滚动渲染

- 弹幕开关、透明度、大小、颜色自定义设置

- 弹幕与倍速、拖拽、暂停状态同步适配

- 弹幕性能优化、节流限流、低端机型适配

### 3\.5 网络与加密安全开发任务

- OkHttp网络请求封装、视频分片下载接口适配

- AES视频分片加密、内存实时解密播放逻辑开发

- 本地缓存视频文件加密存储、解密读取

- 服务端动态密钥拉取、密钥过期更新逻辑

- 接口数据加密、请求签名、防篡改处理

### 3\.6 UI与交互体验开发任务

- 播放器控制栏UI：进度条、播放按钮、缓存进度、时间展示

- 加载状态、缓冲状态、错误状态空页面适配

- 无网络、弱网、加载失败友好交互提示

- 权限申请、存储权限、后台播放权限适配

### 3\.7 性能优化与兼容适配任务

- 内存泄漏检测与修复（播放器、弹幕、网络线程）

- 卡顿优化、帧率监控、ANR修复

- 高低端机型适配、功耗优化、后台播放保活

- Android新版本系统特性适配、分区存储适配

## 四、分阶段开发落地流程（5阶段完整落地）

### 阶段一：基础架构搭建与环境集成（1\-2天）

**阶段目标**：完成项目初始化、技术栈全部依赖集成、基础封装，可正常编译运行

**阶段工作内容**：

1. 创建Kotlin项目，配置Gradle，引入Jetpack、ExoPlayer、FFmpeg、DanmakuFlameMaster、OkHttp全部依赖

2. 搭建MVVM基础架构、基类封装、协程与Flow基础配置

3. 封装OkHttp网络框架、全局异常拦截、日志工具

4. 开发AES加密基础工具类、密钥管理基础逻辑

5. 权限管理、全局工具类初始化

**阶段交付**：可编译项目架构、基础工具类、网络框架、加密工具可用

### 阶段二：核心播放器能力开发（2\-3天）

**阶段目标**：实现完整视频播放核心能力，满足基础点播需求

**阶段工作内容**：

1. 封装全局单例ExoPlayer管理器，绑定页面生命周期

2. 开发基础播放、暂停、进度拖拽、多格式播放能力

3. 实现倍速、静音、循环、记忆播放位置功能

4. 适配HLS/MP4/FLV主流视频格式

5. 开发播放异常重试、错误提示机制

6. 完成横竖屏切换、手势控制交互

**阶段交付**：独立可用的视频播放器组件，可正常播放在线/本地视频

### 阶段三：缓存、FFmpeg辅助、弹幕功能开发（2\-3天）

**阶段目标**：完善视频辅助能力，实现弹幕交互、格式兼容、离线缓存

**阶段工作内容**：

1. 集成FFmpeg，完成特殊视频格式兼容、帧截图、封面提取功能

2. 开发ExoPlayer离线分片缓存、断点续传、缓存管理功能

3. 集成DanmakuFlameMaster，实现弹幕加载、同步、渲染展示

4. 开发弹幕设置功能，适配倍速、拖拽状态同步

5. 优化弹幕性能，解决卡顿、堆积问题

**阶段交付**：支持离线缓存、全格式兼容、完整弹幕交互的播放器

### 阶段四：AES加密与安全能力落地（1\-2天）

**阶段目标**：完成视频资源加密、传输加密、本地存储加密，实现商用安全标准

**阶段工作内容**：

1. 对接服务端，实现动态密钥拉取、定时更新逻辑

2. 开发视频分片AES加密、内存实时解密播放逻辑

3. 实现本地缓存视频文件加密存储、安全读取

4. 完成网络请求签名、防篡改、防调试适配

5. 安全漏洞自查：密钥防硬编码、防抓包、防本地扒资源

**阶段交付**：具备完整安全防护能力的视频播放系统

### 阶段五：UI完善、性能优化、兼容适配、测试上线（2天）

**阶段目标**：完成体验优化、机型适配、bug修复，达到上线标准

**阶段工作内容**：

1. 完善播放器UI、状态页、加载动画、交互细节

2. 全局内存泄漏检测、卡顿优化、功耗优化

3. 高低端机型、新旧系统版本兼容适配

4. 后台播放保活、权限适配、分区存储适配

5. 全功能测试、异常场景测试、压力测试、bug修复

**阶段交付**：完整可上线的商用视频播放APP模块

## 五、上线前核心验收标准

- 播放无黑屏、无花屏、无卡顿、无内存泄漏、无ANR崩溃

- 弹幕同步精准，无错位、堆积、闪烁问题

- 加密机制有效，抓包无法获取完整视频资源，本地无明文视频文件

- 缓存断点续传正常，横竖屏、手势、倍速功能正常

- 高低端机型、主流Android版本适配兼容正常

## 六、UI界面开发要求与核心注意事项

本章节针对视频播放APP专属UI场景，明确播放器界面、控制栏、状态展示、手势交互、横竖屏适配、弹幕UI、机型适配的**强制开发规范与避坑要点**，解决视频APP常见的界面拉伸、适配错乱、交互卡顿、层级遮挡等问题。

### 6\.1 整体UI设计规范要求

- 严格遵循Android原生设计规范，适配**沉浸式状态栏**，播放页面全屏状态下隐藏状态栏、导航栏，视觉无黑边、白边。

- 全局统一尺寸、色值、字体规范，播放器控件圆角、间距、图标样式统一，杜绝界面风格杂乱。

- 区分普通页面与播放页面UI逻辑，播放页面以视频画面为核心，所有控件轻量化，避免过度UI遮挡观影视野。

- 支持深色/浅色模式自适应切换，控制栏、文字、图标颜色随系统模式自动适配，保证明暗场景下清晰可见。

### 6\.2 播放器核心界面层级注意事项（重中之重）

- 层级顺序严格固定（从下至上）：**Surface视频画布 → 弹幕图层 → 加载/状态遮罩 → 播放控制栏 → 弹窗提示**，绝对禁止弹幕、控件遮挡视频画面主体。

- 播放器画布统一使用**TextureView优先、SurfaceView备选**：TextureView适配UI动画、层级叠加更流畅，避免SurfaceView独立图层导致的黑屏、闪烁、层级错乱问题。

- 禁止任何View固定写死宽高，播放器画布强制适配屏幕比例，支持16:9、4:3、全屏铺满、原始比例四种显示模式，无拉伸、无变形、无留白黑边。

- 播放页面根布局禁止滚动嵌套，避免视频画面滑动偏移、触控冲突、画面闪烁。

### 6\.3 播放控制栏UI与交互注意事项

- 控制栏采用**自动隐藏机制**：播放无操作3s后自动隐藏全部控件，点击画面唤起控件；暂停状态永久显示控制栏，提升操作感知。

- 进度条UI规范：区分**播放进度、缓存进度、拖拽进度**三层色值，缓存进度浅于播放进度，视觉层级清晰；进度条触摸区域放大，避免点击拖拽失灵。

- 功能按钮布局合理：播放/暂停、倍速、全屏、缓存、选集、音量按钮分区排布，避免按钮拥挤、误触；禁止过小触控区域（触控区域不小于48dp）。

- 时间显示格式统一：当前播放时间/总时长固定“00:00/00:00”格式，毫秒级进度同步更新，无时间卡顿、跳变问题。

- 全屏/半屏状态控件差异化展示：全屏状态展示更多功能（投屏、弹幕设置、画质选择），半屏状态精简控件，保证界面简洁。

### 6\.4 手势交互UI适配要求

- 严格区分三大手势区域，无触控冲突：左侧上下滑动调节亮度、右侧上下滑动调节音量、左右滑动拖拽播放进度。

- 手势操作必须搭配**实时UI提示**：亮度、音量调节时弹出进度浮窗，拖拽进度时实时展示跳转时间，操作反馈直观。

- 手势适配横竖屏状态，全屏状态手势灵敏度适度调高，半屏状态避免与页面滑动冲突。

- 禁止手势误触发：短点击判定为唤起/隐藏控制栏，长滑动才触发进度、音量、亮度调节，杜绝单次点击误操作。

### 6\.5 弹幕UI专属注意事项

- 弹幕文字大小、行间距自适应屏幕尺寸，不同分辨率手机弹幕展示均匀，不会过大遮挡画面、过小看不清。

- 弹幕默认半透明展示，避免纯色文字过于刺眼；支持用户自定义透明度、字号、颜色，UI设置面板简洁直观。

- 屏蔽顶部、底部控制栏区域弹幕，防止弹幕遮挡进度条、功能按钮，兼顾观影体验与操作体验。

- 加载中、缓冲、暂停状态弹幕渲染不卡顿，界面切换时弹幕平滑暂停/恢复，无突兀闪烁。

### 6\.6 状态页UI适配规范

- 全覆盖状态场景UI：加载中、缓冲中、网络异常、播放失败、无网络、视频失效、缓存为空，每个状态对应独立美观空页面，禁止原生空白黑屏。

- 缓冲状态展示动态加载动画\+缓冲进度文字，让用户感知加载进度，避免误以为APP卡死。

- 异常状态页配置重试按钮、文案提示清晰（如“网络异常，点击重试”“视频资源失效”），交互友好。

- 弱网、流量播放场景弹出轻量提示弹窗，支持用户选择继续播放/暂停，无强制阻断体验。

### 6\.7 横竖屏切换UI核心避坑点

- 横竖屏切换时**UI无重置、进度不丢失、状态不刷新**，禁止切换画面黑屏、进度归零、控件闪烁。

- 全屏状态自动隐藏系统导航栏、状态栏，沉浸式观影；退出全屏自动恢复页面原有UI布局，无布局错乱。

- 切换过程动画平滑过渡，无生硬跳转；所有控件位置、大小随屏幕尺寸自适应适配。

- 适配刘海屏、挖孔屏、折叠屏：播放画面自动避开刘海、挖孔区域，无画面被裁剪、控件遮挡问题。

### 6\.8 机型适配与性能UI注意事项

- 适配不同分辨率、dpi机型，所有控件使用dp/sp单位，禁止px固定尺寸，避免大屏过小、小屏溢出。

- 低端机型UI降级：关闭复杂动画、减少弹窗渲染、降低弹幕刷新帧率，保证界面流畅不卡顿。

- 禁止播放页面过度绘制：减少冗余布局嵌套、透明遮罩叠加，避免高耗电、界面掉帧。

- 后台切前台、页面返回时，播放器UI状态自动恢复（播放/暂停、控件显示状态），无状态错乱。

### 6\.9 权限与弹窗UI规范

- 存储权限、后台播放权限、悬浮窗权限申请弹窗文案友好，说明权限用途，无强制骚扰用户。

- 所有自定义弹窗居中展示、适配圆角样式，与APP整体UI风格统一，无系统原生突兀弹窗。

- 播放无黑屏、无花屏、无卡顿、无内存泄漏、无ANR崩溃

- 弹幕同步精准，无错位、堆积、闪烁问题

- 加密机制有效，抓包无法获取完整视频资源，本地无明文视频文件

- 缓存断点续传正常，横竖屏、手势、倍速功能正常

- 高低端机型、主流Android版本适配兼容正常

### 6\.9 权限与弹窗UI规范

- 存储权限、后台播放权限、悬浮窗权限申请弹窗文案友好，说明权限用途，无强制骚扰用户。

- 所有自定义弹窗居中展示、适配圆角样式，与APP整体UI风格统一，无系统原生突兀弹窗。

## 七、代码编写统一规范与强制开发要求

本章为项目**强制代码规范**，全员统一执行，适用于所有Kotlin、Jetpack、播放器、网络、加密、弹幕、FFmpeg相关代码编写，目的是统一风格、减少Bug、方便维护、规避音视频专属隐患，适配商用上线标准。

### 7\.1 通用 Kotlin 代码基础规范

- 全部使用**纯Kotlin开发**，禁止混合大量Java代码，仅Native/so底层兼容代码可保留Java。

- 严格遵循Google Kotlin官方编码规范，类名大驼峰、变量/函数小驼峰、常量全大写下划线分隔。

- 杜绝魔法值：播放时长、缓存阈值、重试次数、弹幕数量、超时时间、加密参数全部定义 **const val 常量**。

- 强制空安全：所有可空变量严格使用 ? 与 \!\! 禁止滥用，杜绝播放器空指针崩溃，Lateinit仅用于注入控件，禁止用于播放器实例。

- 函数单一职责：一个函数只做一件事，禁止超长函数（超过80行必须拆分），播放、缓存、解密、弹幕逻辑单独拆分子函数。

- 禁止全局无控制变量：播放器状态、缓存状态、弹幕状态统一通过StateFlow托管，禁止全局var随意修改状态。

- 代码注释规范：全量注释，核心代码（如播放器逻辑、加解密逻辑、FFmpeg命令、弹幕同步逻辑）必须写功能注释，复杂逻辑增加思路注释。方便快速生成文档，避免代码注释缺失。

### 7\.2 MVVM \& Jetpack 架构代码规范（强制）

- 严格分层：**View\(Activity/Fragment\) → ViewModel → Repository → 底层工具/网络/本地**，禁止跨层调用。

- View层：只做UI刷新、控件绑定、用户事件分发，**禁止写任何播放逻辑、网络逻辑、加密逻辑**。

- ViewModel层：只做状态处理、业务调度，不持有View引用、不操作控件，不处理UI。

- Repository层：统一管理网络、缓存、本地数据，是唯一数据源，View/ViewModel禁止直接调用OkHttp、文件读写。

- 所有异步逻辑**强制使用 Kotlin 协程 \+ Flow**，禁止Handler、Thread、RxJava混用。

- 页面生命周期全部绑定Lifecycle，播放器、弹幕、网络请求自动跟随页面销毁取消，杜绝内存泄漏、后台残留播放。

- UI状态全部密封类封装（Loading/Success/Error/Empty），禁止零散变量标记状态。

### 7\.3 ExoPlayer 播放器专属代码规范（核心）

- 全局**单例ExoPlayer**，统一PlayerManager管理，禁止每个页面new实例，统一初始化、release、reset逻辑。

- 播放器所有监听（播放状态、缓冲、进度、错误）统一封装，禁止页面零散添加监听。

- 播放进度更新统一使用 **ProgressFlow 定时推送**，禁止Handler循环轮询，减少耗电与卡顿。

- 视频切换逻辑固定：reset \-\> 设置MediaItem \-\> prepare \-\> play，禁止随意无序调用API。

- 播放异常统一拦截：解码异常、网络异常、资源异常统一封装错误码，统一重试、弹窗提示逻辑。

- 缓存逻辑全部通过ExoPlayer DownloadManager封装，禁止自行写文件下载逻辑，避免缓存错乱、断点失效。

- 横竖屏切换、页面重建**不销毁播放器实例**，只保存/恢复播放进度与状态。

### 7\.4 FFmpeg 音视频代码规范

- 所有FFmpeg执行逻辑全部放在**IO协程子线程**，严禁主线程调用，杜绝ANR。

- FFmpeg命令统一工具类封装，禁止业务代码中拼接命令，统一参数、超时、回调模板。

- 每次FFmpeg执行完毕**必须释放native资源、销毁会话**，防止OOM内存堆积。

- 异常视频兼容逻辑做降级封装：优先ExoPlayer硬解，失败后自动调用FFmpeg软解，禁止硬编码判断机型。

- 视频截图、帧提取、裁剪功能统一返回密封状态类，区分成功、失败、取消状态。

### 7\.5 弹幕 DanmakuFlameMaster 代码规范

- 弹幕初始化、启动、暂停、销毁全部跟随播放器生命周期，播放暂停则弹幕暂停，页面销毁立即停止渲染。

- 弹幕时间轴同步逻辑统一封装工具类，倍速、拖拽、跳转自动校准时间戳，禁止零散同步代码。

- 弹幕数据解析统一预处理，过滤异常时间戳、重复数据、空数据，避免渲染崩溃。

- 低端机型性能判断统一封装，自动降低弹幕帧率、限制最大条数，禁止硬编码机型判断。

- 页面退出、视频切换**强制清空弹幕队列**，杜绝弹幕残留、错乱、内存泄漏。

### 7\.6 OkHttp 网络代码规范

- 全局唯一OkHttpClient单例，统一拦截器、超时、重试、请求头、日志配置，禁止多处new Client。

- 区分普通接口超时、视频分片下载超时，两套超时配置分开封装。

- 所有网络请求统一封装在Repository，Activity/Fragment禁止直接发起网络请求。

- 所有网络请求**绑定生命周期自动取消**，页面关闭终止未完成请求，杜绝无用流量、后台回调空指针。

- 统一网络异常封装：无网、超时、4xx/5xx、解析失败，全局统一Toast/状态页处理。

### 7\.7 AES 加密/解密代码规范（安全强制）

- 加解密逻辑**全部封装独立工具类**，禁止业务代码散落加解密逻辑。

- 禁止密钥、IV硬编码明文，必须通过Native加密存储或服务端动态下发。

- 严格使用 AES\-128\-CBC 模式，禁止ECB模式，所有加密参数统一全局常量配置。

- 视频分片解密必须**内存解密、边解边播**，禁止落地明文文件，杜绝资源泄露。

- 解密失败统一捕获异常，自动重试、终止播放、提示资源失效，禁止崩溃。

- 所有加解密操作放在子线程，禁止主线程加解密造成卡顿。

### 7\.8 UI 布局代码规范

- 全局使用 ViewBinding，禁止 findViewById、手写绑定，杜绝控件空指针。

- 布局层级尽量扁平化，播放器页面布局嵌套不超过3层，避免过度绘制、掉帧。

- 所有尺寸使用 dp/sp，禁止px，适配全机型分辨率。

- 播放器、弹幕、控制栏层级固定写死，禁止动态随意修改Z轴层级。

- 控件点击事件统一lambda规范，禁止匿名内部类，减少内存开销。

### 7\.9 性能与内存代码强制规范

- 所有资源：播放器、弹幕线程、FFmpeg会话、网络请求、定时器，**必须成对释放**，谁创建谁销毁。

- 禁止静态持有Activity/Fragment/View实例，杜绝严重内存泄漏。

- 循环播放、弹幕滚动、进度刷新等高频逻辑，严格控制帧率与执行频率。

- 大文件、视频字节流、Bitmap资源使用完毕立即回收，不常驻内存。

- 所有日志上线前统一控制开关，禁止线上打印密钥、视频地址、用户隐私信息。

### 7\.10 打包、混淆、版本规范

- 正式包必须开启混淆、资源压缩，过滤无用资源与调试代码。

- 播放器、FFmpeg、加密核心类禁止混淆，避免功能失效。

- 区分测试包/正式包环境，域名、密钥、日志、调试功能隔离，禁止测试代码混入正式包。

## 八、项目全局常量 \+ 主题颜色规范（可直接落地）

本节为项目**强制统一常量、UI主题色值**，杜绝魔法值、色值散乱，完全适配视频播放器业务、UI交互、加密、性能适配场景，所有内容可直接新建 Constant、Color 资源文件投入开发。

### 8\.1 项目必备全局常量（Kotlin 常量类）

统一放入 **AppConstants\.kt**，全部使用 const val 定义，全局复用，禁止业务代码手写参数。

#### 8\.1\.1 播放器核心常量（ExoPlayer 专属）

- 播放器倍速档位：0\.25f、0\.5f、0\.75f、1\.0f、1\.25f、1\.5f、1.75f、2\.0f、2.5f、3\.0f、3\.5f、4\.0f、5\.0f（常规视频通用档位）

- 自动隐藏控制栏延时：3000ms（3秒无操作隐藏）

- 播放异常最大重试次数：3次

- 播放器缓冲阈值：初始缓冲 1500ms、最大缓冲 5000ms

- 进度刷新间隔：50ms（保证进度丝滑不卡顿）

- 记忆播放最小记录时长：3000ms（播放超3秒记录进度，避免误记录）

- 视频显示模式：原始比例、16:9、4:3、全屏铺满（4种模式枚举）

#### 8\.1\.2 离线缓存常量

- 视频缓存根目录名称：video\_cache（独立隔离目录，避免系统清理）

- 单视频分片大小：1024 \* 1024 \* 2（2M/分片，适配AES分片加密）

- 最大缓存空间：1024 \* 1024 \* 1024 \* 5L（5G）

- 缓存自动清理阈值：4\.5G（达到阈值自动清理最早缓存）

- 断点续传超时时间：60s

#### 8\.1\.3 弹幕 Danmaku 常量

- 单屏最大弹幕数量：80条（低端机型自动降级为40条）

- 弹幕默认字号：14sp、大号16sp、小号12sp

- 弹幕默认透明度：0\.8f（0\~1f区间）

- 弹幕行间距：25dp

- 弹幕渲染帧率：高端机60fps、低端机30fps

- 弹幕数据预加载偏移：5000ms（提前5秒加载后续弹幕）

#### 8\.1\.4 网络 OkHttp 常量

- 普通接口超时：15s

- 视频分片下载超时：30s

- 网络重试间隔：2000ms

- 连接池最大空闲连接：5

- 空闲连接存活时间：60s

#### 8\.1\.5 AES 加密安全常量

- 加密模式：AES\-128\-CBC（固定商用模式）

- 密钥长度：16位（128bit）

- IV偏移量：固定16位向量（全局统一，禁止随意修改）

- 密钥过期时间：24 \* 60 \* 60 \* 1000（24小时自动更新密钥）

- 加密分片对齐大小：16字节（AES加密强制对齐）

#### 8\.1\.6 手势交互常量

- 手势最小滑动判定距离：10dp（防误触）

- 亮度/音量调节灵敏度：0\.02f/每dp滑动

- 点击事件最大判定时长：200ms（区分点击/滑动）
- 长按事件最大判定时长：1000ms（区分长按事件）

#### 8\.1\.7 机型适配、权限常量

- 低端机CPU核心数阈值：4核（低于阈值自动降级UI、帧率）

- Android分区存储适配版本：Build\.VERSION\_CODES\.Q（10\.0）

- 后台播放权限适配版本：Build\.VERSION\_CODES\.S（12\.0）

### 8\.2 项目主题颜色规范（亮色/暗色双主题）

统一放入 **res/values/colors\.xml、values\-night/colors\.xml**，适配视频APP观影属性，深色为主、浅色为辅，降低观影刺眼问题，全局UI统一复用。

#### 8\.2\.1 核心主题色（品牌主色）

- **主色（播放按钮、选中状态、进度条）**：\#FF1677FF（通透科技蓝，视频APP通用主流色）

- 主色浅色：\#FF4096FF（按钮按压、次要选中）

- 主色深色：\#FF0E5ECC（高亮强调、重点按钮）

#### 8\.2\.2 播放器专属色值（重点）

- 视频背景色（默认黑屏）：\#FF000000（纯黑，观影沉浸式最优）

- 控制栏背景色：\#CC000000（80%透明黑，不遮挡画面）

- 播放进度条颜色：主色 \#FF1677FF

- 缓存进度条颜色：\#80FFFFFF（50%透明白）

- 进度条底色：\#33FFFFFF（20%透明白）

- 手势调节浮窗背景：\#99000000

#### 8\.2\.3 弹幕专属色值

- 弹幕默认白色：\#FFFFFFFF

- 弹幕可选颜色：白、黄、红、绿、蓝（\#FFFFDD00、\#FFFF5555、\#FF55FF55、\#FF55AAFF）

- 弹幕描边色：\#33000000（轻微黑描边，保证任何画面下文字清晰）

#### 8\.2\.4 文字颜色（双主题适配）

**亮色模式**：

- 一级文字（标题、时间）：\#FF1D2129

- 二级文字（辅助说明）：\#FF4E5969

- 占位文字：\#FF86909C

**暗色模式**：

- 一级文字：\#FFFFFFFF

- 二级文字：\#FFC9CDD4

- 占位文字：\#FF86909C

#### 8\.2\.5 状态提示色（统一规范）

- 成功色：\#FF52C41A

- 警告色：\#FFFAAD14

- 错误色：\#FFF53F3F

- 提示色：\#FF1677FF

#### 8\.2\.6 分割线、背景色

**亮色模式**：

- 页面背景：\#FFFFFF

- 卡片背景：\#FFF5F7FA

- 分割线：\#FFE5E6EB

**暗色模式**：

- 页面背景：\#FF0A0A0A

- 卡片背景：\#FF1A1A1A

- 分割线：\#FF292929

### 8\.3 规范使用要求

- 所有数值、色值**禁止硬编码**，必须统一引用全局常量、资源色值

- 播放器、弹幕、加密、网络核心参数一经定义，不允许随意修改

- 双主题色值必须成对配置，保证深浅模式切换无UI错乱

- 新增业务参数、色值必须统一归入对应分类，保持项目整洁统一

### 8\.3 规范使用要求

- 所有数值、色值**禁止硬编码**，必须统一引用全局常量、资源色值

- 播放器、弹幕、加密、网络核心参数一经定义，不允许随意修改

- 双主题色值必须成对配置，保证深浅模式切换无UI错乱

- 新增业务参数、色值必须统一归入对应分类，保持项目整洁统一

## 九、项目专属UI动画方案（视频APP专用、低卡顿、可落地）

本章动画**专为视频播放场景定制**，兼容ExoPlayer画布、TextureView层级、弹幕渲染逻辑，杜绝动画导致的黑屏、闪烁、播放卡顿、UI层级错乱，全部采用原生属性动画，适配高低端机型、横竖屏切换，符合项目UI简洁沉浸式风格。

### 9\.1 核心设计原则（视频APP动画强制规范）

- **轻动画、低耗时**：全部动画时长 200ms\~300ms，禁止长耗时动画抢占播放器渲染线程。

- **不干扰播放内核**：播放器画布、弹幕图层禁止叠加位移/缩放动画，仅上层控件做动画。

- **低端机型自动降级**：低端机关闭复杂渐变、帧动画，仅保留基础透明渐变动画，保证60帧流畅。

- **状态联动动画**：动画与播放状态、控件显示状态绑定，无无效动画、无残留动画。

### 9\.2 播放器控制栏显隐动画（核心必做）

解决控制栏突兀显示/隐藏问题，提升沉浸式观影体验，零卡顿、不影响播放。

- **动画类型**：透明度渐变 \+ 垂直位移回弹

- **显示动画**：控制栏从底部外位移20dp→归位，透明度0→1，时长250ms

- **隐藏动画**：控制栏原位→底部外位移20dp，透明度1→0，时长200ms

- **适配规则**：暂停状态禁止隐藏动画，常驻显示；播放无操作自动触发隐藏动画

- **避坑点**：动画过程禁止刷新播放器画布，避免画面闪烁

### 9\.3 手势交互反馈动画（亮度/音量/进度）

手势调节时的浮窗提示动画，交互反馈直观、柔和不刺眼。

- **动画类型**：透明度渐变 \+ 缩放微动

- **出现动画**：浮窗中心缩放0\.9f→1\.0f，透明度0→1，时长200ms

- **消失动画**：透明度1→0，时长300ms，无突兀消失

- **适配场景**：左右滑进度、左侧亮度、右侧音量调节全局生效

### 9\.4 播放按钮状态动画（播放/暂停切换）

居中大播放按钮、控制栏播放按钮状态切换动效，简洁高级。

- **动画类型**：属性形变动画 \+ 按压缩放

- **点击按压动画**：按钮1\.0f→0\.95f→1\.0f，轻微回弹，提升点击质感

- **状态切换动画**：播放/暂停图标平滑切换，透明度过渡，无瞬间闪变

- **居中暂停按钮**：视频暂停时居中播放按钮淡入，播放时淡出

### 9\.5 视频加载/缓冲动画（空状态核心）

替代生硬静态Loading，适配视频缓冲、网络加载场景。

- **动画类型**：环形匀速旋转动画 \+ 透明度呼吸动画

- **缓冲Loading**：环形旋转，匀速360°循环，无卡顿、无抖动

- **弱网缓冲提示**：文字“缓冲中\.\.\.”搭配慢速呼吸渐变动画

- **适配规则**：播放正常后立即停止动画，释放CPU资源

### 9\.6 横竖屏切换过渡动画

解决横竖屏切换生硬跳转问题，适配全屏观影体验。

- **动画类型**：全局画布平滑缩放过渡

- **动画逻辑**：切换瞬间画面轻微缩放适配屏幕，控件同步渐变显隐

- **避坑重点**：动画全程**不重建播放器实例**，仅UI适配，无进度丢失、无黑屏

### 9\.7 弹幕设置弹窗动画

弹幕大小、透明度、开关设置弹窗动效，贴合视频UI风格。

- **动画类型**：底部弹出平移动画 \+ 背景变暗渐变

- **弹出**：从屏幕底部向上平移入场，透明度0→1，时长250ms

- **收起**：向下平移退场，透明度1→0，时长200ms

- **背景动画**：弹窗显示时背景黑色透明度从0→0\.6渐变，聚焦弹窗操作

### 9\.8 视频切换动画（上一集/下一集）

切换视频时的页面过渡，避免画面瞬间刷新突兀感。

- **动画类型**：画面淡入淡出过渡

- **切换逻辑**：旧视频画面透明度1→0，新视频画面0→1，交叉渐变

- **优势**：配合ExoPlayer预加载，实现无缝切换观感

### 9\.9 缓存进度动画

进度条缓存加载动态动效，替代静态进度填充，提升动态感知。

- **动画类型**：属性值平滑更新动画

- **逻辑**：缓存进度不瞬间跳变，匀速平滑填充进度条，观感更流畅

- **适配场景**：视频分片下载、离线缓存列表进度展示

### 9\.10 禁止使用的动画（项目强制避坑）

- 禁止对**TextureView/SurfaceView播放器画布**做缩放、位移、旋转动画（极易黑屏、闪烁、解码异常）

- 禁止帧动画、GIF动画高频循环播放（高耗电、占用播放线程资源）

- 禁止超长动画（时长\>500ms）、弹簧抖动动画（破坏沉浸式极简风格）

- 禁止弹幕图层叠加动画（会导致弹幕错位、渲染卡顿）

### 9\.11 动画全局统一常量（复用8\.1常量规范）

所有动画时长统一全局常量，禁止硬编码时长：

- 动画快速时长：200ms（退场、点击反馈）

- 动画标准时长：250ms（弹窗、控件显隐）

- 动画慢速时长：300ms（渐变退场、呼吸动画）

- 动画插值器：默认匀速插值器、轻微减速插值器（无突兀回弹）

## 十、抖音式竖屏短视频播放专属技术支持方案

本章针对**抖音/快手同款竖屏短视频沉浸式播放**场景，梳理所需全套专属技术、差异化适配、核心优化、必填能力，区别于传统长视频播放逻辑，补齐短视频滑动、预加载、无缝衔接、极致流畅、低延迟核心能力，完全适配本项目现有技术栈（Kotlin\+ExoPlayer\+Jetpack）。

### 10\.1 短视频与长视频核心技术差异（关键）

- 长视频：侧重缓存、进度记忆、倍速、横竖屏、断点续传；**不追求极速切换、秒开、无缝衔接**

- 短视频：侧重**秒开、预加载、上下滑动无缝切换、零卡顿、预渲染、静音自动播放**，进度记忆弱化，沉浸式全屏独占播放

- 短视频核心痛点：滑动掉帧、切换黑屏、首帧慢、连续播放内存暴涨、手势冲突、声音切换突兀

### 10\.2 短视频必备核心技术栈（基于现有架构扩展）

#### 10\.2\.1 播放器内核专属优化（ExoPlayer 魔改适配）

- **多实例播放器池技术**：放弃长视频单例播放器，采用「3个播放器缓存池」（当前播放\+上一个预存\+下一个预加载），实现上下滑动**零黑屏、无缝衔接**，抖音核心底层方案。

- 短视频专属播放策略：**静音预加载、自动播放、页面可见性联动播放/暂停**

- 首帧极速渲染：关闭多余解码校验，开启ExoPlayer极速渲染模式，首帧渲染控制在100ms内

- 智能释放策略：滑动离开后延迟回收播放器，快速回滑无需重新加载，提升复用体验

- 短视频循环播放：单视频无限循环，贴合短视频产品形态

#### 10\.2\.2 列表滑动容器技术

- **ViewPager2 / RecyclerView Pager**：实现标准上下一页全屏滑动效果，禁用横向滑动，匹配抖音交互

- 页面精准监听：OnPageChangeCallback 精准区分滑动状态（拖拽、滑动中、滑动完成）

- 滑动节流防抖：防止快速连滑导致多次预加载、播放器实例错乱、内存峰值暴涨

- 全屏独占布局：每个Item铺满屏幕，无留白、无适配拉伸，强制竖屏9:16比例

#### 10\.2\.3 智能预加载技术（短视频流畅核心）

- 可视区域预加载：当前播放、预加载下1个、缓存上1个，始终保持3个就绪视频

- 弱网/流量智能降级：WiFi下全额预加载，流量下仅预加载首帧、降低码率，节省流量

- 预加载时机优化：页面静止、无滑动操作时触发预加载，滑动中暂停多余预加载，避免抢占资源卡顿

- 预加载缓存阈值：短视频单文件预加载缓冲 2000ms 即可就绪，无需长视频大缓冲

#### 10\.2\.4 沉浸式全屏与系统适配技术

- 极致沉浸式：隐藏状态栏、导航栏，适配刘海屏、挖孔屏、水滴屏、折叠屏全屏显示

- 固定竖屏锁定：短视频页面强制竖屏，禁止横竖屏旋转，杜绝界面跳转错乱

- 屏幕常亮：播放短视频时自动保持屏幕常亮，暂停/退出页面关闭常亮

- 系统窗口适配：规避系统手势、状态栏遮挡视频画面

#### 10\.2\.5 音频焦点管理（解决声音冲突）

- 系统音频焦点监听：切后台、其他APP播放音频时自动暂停视频、静音

- 滑动切换音频平滑启停：上一个视频静音淡出、下一个视频有声淡入，无爆音、无突兀切换

- 全局静音/有声状态记忆：跟随用户设置，统一所有短视频播放状态

#### 10\.2\.6 短视频专属缓存策略

- 短时缓存机制：短视频不需要永久缓存，采用**临时内存缓存\+短时磁盘缓存**

- 智能淘汰策略：自动淘汰30分钟未访问、滑动划过的视频缓存，严控包体积与内存

- 分片极速加载：适配短视频HLS/TS分片，小文件快速加载，秒开率拉满

#### 10\.2\.7 手势交互增强技术

- 双击点赞手势：精准双击识别，防误触，搭配点赞缩放动画

- 左右滑动交互：左侧评论、右侧个人主页弹窗滑出，与上下滑动播放不冲突

- 单击启停播放：单击视频暂停/继续播放，隐藏/显示操作UI

- 手势优先级分级：上下滑动翻页 \> 单击播放 \> 双击点赞 \> 左右侧滑弹窗

#### 10\.2\.8 低性能机型降级技术

- 低端机关闭三播放器池，改为单实例复用，降低内存占用

- 降级预加载数量：仅预加载下1个视频，减少CPU/网络消耗

- 降低解码帧率：锁定30帧播放，杜绝低端机掉帧卡顿

- 关闭多余动画、点赞特效、弹幕高频渲染，保证基础流畅度

### 10\.3 短视频配套UI与动画专属技术

- 全屏沉浸式UI结构：视频画布底层，右侧头像、关注、点赞、评论、分享悬浮层置顶

- 无缝切换动画：页面滑动无黑屏、无闪烁，画面跟随手势位移过渡

- 点赞粒子动画、爱心缩放动画、按钮按压微动动画

- 底部文案、话题标签、音乐文案滚动动画

- 弹窗侧滑动画：评论弹窗、个人主页弹窗左右平滑弹出收起

### 10\.4 短视频必备安全与加密适配

- 短视频分片AES加密：适配小文件快速加解密，不影响秒开速度

- 内存解密播放：禁止短视频明文落地，防止批量爬取、盗录资源

- 链接时效限制：短视频播放链接短期过期，防止永久盗用转发

### 10\.5 短视频专属性能避坑规范（重点）

- 禁止无限创建播放器实例，必须使用播放器对象池复用，杜绝OOM内存溢出

- 滑动过程禁止大量网络请求、解析任务，避免页面卡顿掉帧

- 短视频页面严格禁止布局嵌套冗余，保证滑动帧率稳定60帧

- 及时释放离屏播放器、停止解码线程，防止后台持续耗电

- 禁止短视频播放页面触发系统回收重建，锁定页面状态与播放器实例

### 10\.6 短视频完整能力清单（对标抖音基础版）

- 上下滑动无缝切换视频、秒开播放、静音自动播

- 双击点赞、右侧功能栏（点赞/评论/收藏/分享/关注）

- 视频循环播放、音频平滑切换、静音/有声切换

- 预加载智能缓存、弱网适配、流量节省模式

- 全屏沉浸式播放、屏幕常亮、竖屏锁定

- 短视频弹幕、话题标签、背景音乐滚动展示

- 高低端机型适配、性能降级、帧率稳定

- 加密资源播放、防爬防盗、时效链接

- 播放无黑屏、无花屏、无卡顿、无内存泄漏、无ANR崩溃

- 弹幕同步精准，无错位、堆积、闪烁问题

- 加密机制有效，抓包无法获取完整视频资源，本地无明文视频文件

- 缓存断点续传正常，横竖屏、手势、倍速功能正常

- 高低端机型、主流Android版本适配兼容正常

- 代码完全符合项目统一编码规范，无违规代码、无冗余代码、无内存隐患

- 全局常量、主题色值统一复用，无魔法值、无散乱色值，UI风格统一

- 所有UI动画轻量低耗，不卡顿、不黑屏、不干扰播放与弹幕渲染，高低端机型适配正常

## 九、上线前核心验收标准

- 播放无黑屏、无花屏、无卡顿、无内存泄漏、无ANR崩溃

- 弹幕同步精准，无错位、堆积、闪烁问题

- 加密机制有效，抓包无法获取完整视频资源，本地无明文视频文件

- 缓存断点续传正常，横竖屏、手势、倍速功能正常

- 高低端机型、主流Android版本适配兼容正常

- 代码完全符合项目统一编码规范，无违规代码、无冗余代码、无内存隐患

- 全局常量、主题色值统一复用，无魔法值、无散乱色值，UI风格统一

- 播放无黑屏、无花屏、无卡顿、无内存泄漏、无ANR崩溃

- 弹幕同步精准，无错位、堆积、闪烁问题

- 加密机制有效，抓包无法获取完整视频资源，本地无明文视频文件

- 缓存断点续传正常，横竖屏、手势、倍速功能正常

- 高低端机型、主流Android版本适配兼容正常

- 代码完全符合项目统一编码规范，无违规代码、无冗余代码、无内存隐患

