# 视频APP两大核心页面布局方案（长视频 \+ 短视频 商用最优方案）

本文针对当前视频APP项目，提供**4套商用级稳定布局方案**，分别适配【常规长视频播放页】【抖音竖屏短视频播放页】，全部遵循项目规范：层级≤3、无过度绘制、适配TextureView、不卡顿、不黑屏、手势无冲突、兼容横竖屏与沉浸式状态栏，可直接落地使用。

## 一、方案一：长视频播放页 终极稳定布局（主推）

### 1\. 适用场景

剧集、电影、网课、长视频点播页面（支持横竖屏切换、手势控亮度音量、弹幕、控制栏显隐）。

### 2\. 根布局结构（层级2层，极简不卡顿）

**FrameLayout（根布局）**

- 底层：TextureView（视频渲染画布，铺满父布局）

- 中层：DanmakuView（弹幕图层，匹配视频尺寸）

- 顶层：播放器UI容器（控制栏、进度条、加载状态、手势浮窗）

### 3\. 层级严格顺序（从下至上）

TextureView 视频画布 → 弹幕View → 状态遮罩层 → 底部/顶部控制栏 → 弹窗提示

### 4\. 核心优势

- FrameLayout无测量嵌套，**全程60帧稳定**，无掉帧、无过度绘制

- TextureView优先渲染，支持UI动画、层级叠加，杜绝SurfaceView黑屏闪烁问题

- 弹幕与视频精准对齐，倍速/拖拽不同步问题最少

- 横竖屏切换布局重建量最小，播放进度不丢失、UI不乱窜

- 完美兼容手势识别（亮度/音量/进度），无触控冲突

### 5\. 强制规范与避坑

- 所有控件宽高统一 `match_parent` 铺满，禁止固定尺寸

- 控制栏、进度条使用 **match\_parent\+底部/顶部对齐**，不挤压视频画布

- 加载动画、错误状态页统一覆盖全屏，不改变视频画布尺寸

- 根布局禁止嵌套ScrollView、RecyclerView，防止触控冲突、画面偏移

## 二、方案二：抖音竖屏短视频全屏布局（主推）

### 1\. 适用场景

短视频上下滑动Feed流、沉浸式全屏播放、点赞评论右侧悬浮UI。

### 2\. 整体结构

**ViewPager2（外层滑动容器）** → Item帧布局（单条短视频页面）

**单Item内部结构：FrameLayout（单层根布局）**

- 底层：TextureView 9:16全屏铺满

- 中层：弹幕View（可选）

- 顶层：沉浸式悬浮UI层（右侧功能栏\+底部文案\+音乐图标）

### 3\. 核心优势

- ViewPager2原生滑动手感最接近抖音，防卡顿、防错位

- Item极简FrameLayout，**层级只有2层**，滑动帧率极致稳定

- 右侧点赞、评论、关注控件固定悬浮，不遮挡核心观影区域

- 适配播放器池三实例复用，无缝切换无黑屏

- 强制9:16比例，无拉伸、无留白、无黑边

### 4\. 专属适配规范

- ViewPager2 设置 `offscreenPageLimit = 1`，配合预加载策略

- 禁止Item内部嵌套复杂布局、RecyclerView、多层RelativeLayout

- 所有悬浮UI使用 `wrap_content` \+ 边距控制，不挤压视频画面

- 页面强制竖屏锁定，禁止自动旋转

## 三、方案三：兼容万能布局（适配旧机型/特殊视频）

### 1\. 适用场景

需要兼容奇葩分辨率视频、需要固定比例、低端机型适配、本地视频混合播放页面。

### 2\. 布局结构

**ConstraintLayout（根布局）**

核心：通过比例约束 `layout_dimensionRatio` 锁定视频比例

- 支持动态切换 16:9 / 4:3 / 全屏 / 原始比例

- 所有UI控件通过约束绑定四边，适配刘海屏、挖孔屏

### 3\. 优势与适用场景

- 比例可控，杜绝视频拉伸变形、裁剪异常

- 适配性最强，新旧机型、异形屏全部兼容

- 适合需要**手动切换画面显示比例**的功能页面

### 4\. 避坑点

- ConstraintLayout测量耗时略高于FrameLayout，**短视频Feed流禁止使用**，仅长视频使用

- 禁止过多约束链，保持布局简洁，避免测量卡顿

## 四、方案四：小窗悬浮播放布局（PiP画中画专用）

### 1\. 适用场景

后台小窗播放、悬浮画中画、边刷列表边播放。

### 2\. 布局结构

**FrameLayout 浮动窗口布局**

- 固定小窗比例16:9，固定最小宽高

- 仅保留视频画布 \+ 关闭按钮 \+ 放大按钮

- 隐藏所有复杂控制栏、弹幕、进度UI

### 3\. 核心规范

- 小窗模式强制降低渲染帧率，节省耗电

- 禁止任何动画、弹幕渲染，极致轻量化

- 支持手势拖动悬浮窗位置

## 五、四大布局方案选型总结（直接照选）

|页面场景|推荐布局|核心理由|
|---|---|---|
|长视频播放页（电影/剧集）|方案一 FrameLayout|帧率最高、层级最简、横竖屏最稳|
|抖音短视频Feed流|方案二 ViewPager2\+FrameLayout|滑动丝滑、无缝切换、适配播放器池|
|多比例兼容播放页|方案三 ConstraintLayout|比例可控、异形屏适配完美|
|悬浮小窗播放|方案四 极简FrameLayout|轻量化、低耗电、无卡顿|

## 六、全局布局统一强制规则（适配项目已有规范）

- 所有播放页面布局层级**严格≤3层**，杜绝过度绘制

- 视频画布统一使用 **TextureView 优先**，SurfaceView 降级备用

- 所有尺寸使用 dp/sp，禁止 px，全局适配

- 播放核心区域禁止任何滚动嵌套、手势冲突布局

- UI动画仅作用顶层控件，**不对视频画布、弹幕图层做动画**

> （注：部分内容可能由 AI 生成）
